Refer to the INSTALL file for installation instructions.

./configure
make
make install

To enable the daemon you must enable DBus support by running
configure with --enable-dbus

Check out the man page xcowsay(6) for the documentation.